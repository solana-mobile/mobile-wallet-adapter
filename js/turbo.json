{
    "$schema": "https://turborepo.dev/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["lib/**"]
        },
        "check-types": {
            "dependsOn": ["^build", "^check-types"]
        },
        "clean": {
            "cache": false
        }
    }
}
